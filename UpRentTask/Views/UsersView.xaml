<UserControl x:Class="UpRentTask.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:UpRentTask.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="800"
             d:DesignWidth="1200"
             mc:Ignorable="d">
  <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="60" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <StackPanel Grid.Row="0" Margin="10" Orientation="Horizontal">
      <StackPanel.Resources>
        <Style TargetType="{x:Type Button}">
          <Setter Property="Margin" Value="0,0,10,0" />
        </Style>
      </StackPanel.Resources>
      <Button Width="120" Content="Dodaj" />
      <Button Width="120" Content="Izmjeni" />
      <Button Width="120" Content="Briši" />
    </StackPanel>

    <DataGrid Grid.Row="1"
              Margin="10"
              AutoGenerateColumns="False" ColumnHeaderHeight="40" RowHeight="35">
      <DataGrid.Columns>
        <DataGridTextColumn Width="*" Header="Korisničko ime" />
        <DataGridTextColumn Width="*" Header="Datum kreiranja" />
        <DataGridTextColumn Width="*" Header="Kreirao" />
        <DataGridTextColumn Width="*" Header="Datum izmjene" />
        <DataGridTextColumn Width="*" Header="Izmjenio" />
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>
